<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>TipoDocumento__c</fullName>
    <description>Tipo do documento fiscal do lead (CPF para pessoa física, CNPJ para pessoa jurídica). Alimenta o nome da identificação no Data Cloud para identity resolution (A1.3).</description>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(CPF__c)), &quot;CPF&quot;, IF(NOT(ISBLANK(CNPJ__c)), &quot;CNPJ&quot;, &quot;&quot;))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Tipo de Documento</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
