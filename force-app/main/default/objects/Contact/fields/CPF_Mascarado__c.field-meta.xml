<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CPF_Mascarado__c</fullName>
    <description>CPF mascarado (exibe só os 2 últimos dígitos) para identificação sem expor o documento completo — LGPD/menor privilégio (A9.2). Papéis de atendimento usam este campo, não o CPF__c.</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(CPF__c), &quot;&quot;, &quot;***.***.***-&quot; &amp; RIGHT(CPF__c, 2))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>CPF (mascarado)</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
